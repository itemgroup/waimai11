server:
  port: 8081

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/luckin?useSSL=true&useUnicode=true&characterEncoding=utf8
    username: root
    password: 20211127lmyfkd

# MyBatis配置
mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml
  type-aliases-package: com.amx.luckincoffee.entity